<%= semantic_form_for @order, url: change_store_user_admin_order_path(@order) do |f| %>
  <%= f.inputs do %>
    <%= f.input :store_user_id, as: :select, collection: StoreUser.all %>
  <% end %>
  <%= f.actions %>
<% end %>
